# Robot Framework Web Automation (POM Demo)

This project demonstrates a Page Object Model (POM) structure using Robot Framework + SeleniumLibrary.

## Setup

```bash
pip install -r requirements.txt

Add python interpreter and create a virtual environment.
robot tests
